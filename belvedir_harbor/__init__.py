"""Belvedir ↔ Harbor bridge: an external Harbor agent that plays the model
under test on Belvedir environments, plus the verifier those environments
ship in every task (verify.py)."""

__version__ = "0.1.0"
