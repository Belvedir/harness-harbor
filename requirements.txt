# Harbor with its cloud sandbox backends (the Belvedir sandbox has no Docker
# daemon, so suites run on Harbor's Modal backend). Pin the minor: the task
# schema and CLI flags this driver relies on are Harbor 0.22's.
harbor[modal]>=0.22,<0.23
# The Belvedir external agent (HARBOR_AGENT=belvedir — the Harbor runner) is
# VENDORED in ./belvedir_harbor (mirror of the platform repo's harbor/ package,
# synced by scripts/sync-harness-harbor.sh there — the platform repo is private,
# so it can't be pip-installed from git). Its tracing SDK:
belvedir>=0.6
